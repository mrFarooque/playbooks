---
- name: run my local script
  hosts: shdemo_nuc
  gather_facts: false
  become_user: yes

  vars_prompt: []

  tasks:
    - name: Run the script
      ansible.builtin.shell: ./script.sh >> output2.txt
      args:
        chdir: /home/farooq/test